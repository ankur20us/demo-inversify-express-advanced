/**
 *  This class loads all the Controller Classes that we have
 */
import '../middlewares/custom.middleware.functional';
import '../middlewares/custom.middleware.controller';
import '../controllers/user.controller';
